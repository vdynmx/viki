%h1 New Wiki
= form_for @wiki do |f|
  = f.label :title
  = f.text_field :title
  = f.label :body
  = f.text_area :body
  - if current_user.role == "admin" || current_user.role == "paid"
    = f.label :Private
    = f.check_box :private
  = f.submit "Save"